#spring.datasource.url: jdbc:${CLEARDB_DATABASE_URL:mysql://localhost:3306/blog_engine}
spring.datasource.url: jdbc:mysql://localhost:3306/box5
spring.datasource.username: root
spring.datasource.password: password

spring.jpa.hibernate.ddl-auto: validate
spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql: true

spring.jackson.default-property-inclusion: non_null

spring.servlet.multipart.max-file-size: 1200KB
spring.servlet.multipart.max-request-size: 1200KB

server.port: ${PORT:8080}

someParameter:
  value: 5463

minutesBeforeDeleteCaptcha: 60

spring:
  mail:
    host: smtp.yandex.ru
    username: testSendSpringMail
    password: testSendSpringMail123
    port: 465
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true

blog.title: DevPub
blog.subtitle: Рассказы разработчиков
blog.phone: +7 926 111-22-33
blog.email: mail@mail.ru
blog.copyright: Иван Иванов
blog.copyrightFrom: 2005